var prompt = require('prompt-sync')();
var len = prompt("Enter the length of the rectangle: ");
var width = prompt("Enter the width of the rectangle: ");
var area = len * width;
console.log("The area of the rectangle is: " + area); // Output: The area of the rectangle is: [calculated area]